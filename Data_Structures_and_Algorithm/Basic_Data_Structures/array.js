const name = ["nouman", "merab", "bubu"]; //using array literals

const city = new Array("Lahore", "Islamabad", "Karachi"); //using Array constructor

const age = [] //empty array

const cnic = new Array(); //empty array

for (let i in array){
    console.log(i)
}